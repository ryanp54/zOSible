---
  - hosts: zsystem
    gather_facts: no
    vars_files:
      - ./vars/last_args.yaml
    environment:
      "{{environment_vars}}"
        
    collections:
      - ibm.ibm_zos_core
    
    tasks:
       - include: ask_pass.yaml
       - include: upload.yaml
       - include: remove.yaml
         when: create_result.changed and copy_result.failed
