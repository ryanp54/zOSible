---
  - hosts: zsystem
    gather_facts: no
    environment:
      "{{environment_vars}}"

    collections:
      - ibm.ibm_zos_core

    tasks:
      - include: ask_pass.yaml

      - name: List matching data sets
        zos_find:
          patterns:
            - "{{ query }}"